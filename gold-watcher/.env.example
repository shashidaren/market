# gold-watcher — copy this file to .env and fill in real values
# Real values: keep them in your password manager, NEVER commit them.
# config.py loads this file automatically (or reads environment variables).

# Telegram bot for gold price alerts (get token from @BotFather)
TELEGRAM_TOKEN=123456789:your-telegram-bot-token-here
TELEGRAM_CHAT_ID=your-chat-id-here
