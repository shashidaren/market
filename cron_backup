root@webscrap:/opt/market# crontab -l 
# Edit this file to introduce tasks to be run by cron.
# 
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
# 
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').
# 
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
# 
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
# 
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
# 
# m h  dom mon dow   command



*/30 * * * * cd /opt/market && /usr/bin/python3 collector.py >> /var/log/webscrap-collector.log 2>&1
5,35 * * * * cd /opt/market && /usr/bin/python3 scorer.py >> /var/log/webscrap-scorer.log 2>&1

# --- fx-signal-pipeline (managed by install_cron.sh) ---
# --- end fx-signal-pipeline ---

# ── Bursa announcements pipeline ──
*/10 * * * *   cd /opt/market/announcements_signal && /usr/bin/python3 collector.py >> /var/log/webscrap-bursa-collector.log 2>&1
2-59/10 * * * * cd /opt/market/announcements_signal && /usr/bin/python3 parse_detail.py --days 1 >> /var/log/webscrap-bursa-parse.log 2>&1
3-59/10 * * * * cd /opt/market/announcements_signal && /usr/bin/python3 signals_engine.py --days 2 >> /var/log/webscrap-bursa-scorer.log 2>&1
4-59/10 * * * * cd /opt/market/announcements_signal && /usr/bin/python3 telegram_bot.py >> /var/log/webscrap-bursa-telegram.log 2>&1


* * * * * BASH_ENV=/opt/market/fx_signal/.env /opt/market/fx_signal/run_pipeline.sh





# --- indices-signal-pipeline ---
#*/30 * * * * BASH_ENV=/opt/market/indices_signal/.env /opt/market/indices_signal/run_pipeline.sh
# --- end indices-signal-pipeline ---

